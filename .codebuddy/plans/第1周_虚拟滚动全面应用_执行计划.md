# 第1周执行计划：虚拟滚动全面应用

## 目标
优化3个长列表组件，提升性能5-10倍，解决长作品（50+角色/100+设定/50+节点）卡顿问题

---

## 任务清单

| 组件 | 渲染位置 | 当前实现 | 优化方案 | 预估时间 | 性能提升 |
|------|----------|----------|----------|----------|----------|
| CharacterCreator | 第283行 | `characters.filter().map()` | VirtualList | 1.5小时 | 5-10x |
| WorldBuilder | 第351行 | `filteredSettings.map()` | VirtualList | 1.5小时 | 5-10x |
| PlotWeaver | 待定位 | `plotNodes.map()` | VirtualList | 2小时 | 5-10x |

---

## Day 1-2: CharacterCreator 优化

### 执行步骤

#### Step 1: 分析当前实现（15分钟）
- **文件**: `components/CharacterCreator.tsx`
- **位置**: 第283行
- **问题**: `project.characters.filter(...).map(...)` 全量渲染
- **影响**: 30+角色时滚动卡顿，搜索延迟

#### Step 2: 重构为虚拟滚动（45分钟）

**修改内容**:

```typescript
// 1. 添加导入
import { VirtualList } from '../VirtualList';

// 2. 替换第282-320行的渲染逻辑
<div className="flex-1 overflow-y-auto p-2 space-y-2">
    {/* 空状态 */}
    {project.characters.filter(c => c.name.includes(searchQuery) || c.role.includes(searchQuery)).length === 0 && (
        <p className="text-slate-500 text-xs p-4 text-center">暂无匹配角色</p>
    )}
    
    {/* 虚拟滚动优化 */}
    {project.characters.filter(c => c.name.includes(searchQuery) || c.role.includes(searchQuery)).length > 0 && (
        <VirtualList
            items={project.characters.filter(c => c.name.includes(searchQuery) || c.role.includes(searchQuery))}
            itemHeight={80}
            height={window.innerHeight - 400}
            className="space-y-2"
            renderItem={(char, idx) => {
                const hasEcho = (project.echoes || []).some(e => e.targetId === char.id && e.status === 'PENDING');
                return (
                    <div
                        key={char.id}
                        onClick={() => { setActiveCharId(char.id); setDraftCharacter(null); }}
                        className={`p-3 rounded-lg cursor-pointer flex items-center gap-3 group transition-all relative overflow-hidden ${
                            activeCharId === char.id 
                                ? 'bg-muse-900/50 border border-muse-500/50' 
                                : 'bg-slate-800 border border-transparent hover:bg-slate-750'
                        } ${hasEcho && activeCharId !== char.id ? 'shadow-[0_0_15px_rgba(34,211,238,0.15)] border-cyan-900/50' : ''}`}
                    >
                        {/* Echo指示器 */}
                        {hasEcho && (
                            <div className="absolute left-0 top-0 bottom-0 w-1 bg-cyan-400 shadow-[0_0_8px_rgba(34,211,238,0.8)] animate-pulse"></div>
                        )}
                        
                        {/* 头像 */}
                        <div className={`w-10 h-10 rounded-full bg-slate-700 overflow-hidden flex-shrink-0 border ${
                            hasEcho ? 'border-cyan-500/50' : 'border-slate-600'
                        }`}>
                            {char.imageUrl ? (
                                <img src={char.imageUrl} alt={char.name} className="w-full h-full object-cover" />
                            ) : (
                                <div className="w-full h-full flex items-center justify-center text-slate-500">
                                    <User size={20} />
                                </div>
                            )}
                        </div>
                        
                        {/* 角色信息 */}
                        <div className="flex-1 min-w-0">
                            <div className="font-medium text-sm text-slate-200 truncate">
                                {char.name}
                                {hasEcho && <span className="ml-2 text-[10px] bg-cyan-500/20 text-cyan-300 px-1.5 py-0.5 rounded-full">待处理</span>}
                            </div>
                            <div className="text-xs text-slate-400 truncate">{char.role}</div>
                        </div>
                    </div>
                );
            }}
        />
    )}
</div>
```

#### Step 3: 测试验证（30分钟）

**测试清单**:
- [ ] 添加30+测试角色
- [ ] 验证搜索过滤性能（应无明显延迟）
- [ ] 验证滚动流畅度（应无卡顿）
- [ ] 验证Echo状态指示器正常显示
- [ ] 验证点击选择角色功能正常
- [ ] 检查React DevTools Profiler，确认重渲染减少

**性能指标**:
- 滚动帧率: > 50fps (之前: < 20fps)
- 搜索响应: < 100ms (之前: 300-500ms)

---

## Day 3-4: WorldBuilder 优化

### 执行步骤

#### Step 1: 分析当前实现（15分钟）
- **文件**: `components/WorldBuilder.tsx`
- **位置**: 第351行
- **问题**: `filteredSettings.map(...)` 全量渲染
- **影响**: 100+设定条目时严重卡顿

#### Step 2: 重构为虚拟滚动（45分钟）

**修改内容**:

```typescript
// 1. 添加导入
import { VirtualList } from '../VirtualList';

// 2. 替换第319-378行的渲染逻辑
<div className="flex-1 overflow-y-auto p-2 space-y-2">
    {/* 新建条目表单 - 保持不动 */}
    <div className="p-2">
        <div className="flex gap-2">
            <input
                type="text"
                value={newItemTitle}
                onChange={(e) => setNewItemTitle(e.target.value)}
                placeholder="新建词条标题..."
                className="flex-1 bg-slate-900 border border-slate-700 rounded-md px-3 py-2 text-sm text-white focus:border-muse-500 outline-none"
            />
            <div className="flex gap-1">
                <button
                    onClick={handleManualAdd}
                    disabled={draftLore !== null}
                    title="手动创建"
                    className="bg-slate-700 hover:bg-slate-600 text-white p-2 rounded-md disabled:opacity-50"
                >
                    <BookPlus size={18} />
                </button>
                <button
                    onClick={handleGenerateLore}
                    disabled={isGenerating || draftLore !== null}
                    title="AI 灵感生成"
                    className="bg-muse-600 hover:bg-muse-500 text-white p-2 rounded-md disabled:opacity-50"
                >
                    {isGenerating ? <div className="animate-spin w-4 h-4 border-2 border-white/30 border-t-white rounded-full"></div> : <Plus size={18} />}
                </button>
            </div>
        </div>
    </div>
    
    {/* 虚拟滚动列表 */}
    {filteredSettings.length > 0 && (
        <VirtualList
            items={filteredSettings}
            itemHeight={60}
            height={window.innerHeight - 450}
            className="space-y-2"
            renderItem={(lore, idx) => {
                const hasEcho = (project.echoes || []).some(e => e.targetId === lore.id && e.status === 'PENDING');
                return (
                    <div
                        key={lore.id}
                        onClick={() => { setActiveItemId(lore.id); setDraftLore(null); }}
                        className={`p-3 rounded-lg cursor-pointer flex justify-between items-center group relative overflow-hidden ${
                            activeItemId === lore.id 
                                ? 'bg-muse-900/50 border border-muse-500/50' 
                                : 'bg-slate-800 hover:bg-slate-750 border border-transparent'
                        } ${hasEcho && activeItemId !== lore.id ? 'shadow-[0_0_15px_rgba(34,211,238,0.15)] border-cyan-900/50' : ''}`}
                    >
                        {/* Echo指示器 */}
                        {hasEcho && (
                            <div className="absolute left-0 top-0 bottom-0 w-1 bg-cyan-400 shadow-[0_0_8px_rgba(34,211,238,0.8)] animate-pulse"></div>
                        )}
                        
                        {/* 标题 */}
                        <span className={`font-medium truncate ${hasEcho ? 'text-cyan-100' : 'text-slate-200'}`}>
                            {lore.title}
                        </span>
                        
                        {/* 删除按钮 */}
                        <button
                            onClick={(e) => { e.stopPropagation(); deleteLore(lore.id); }}
                            className="opacity-0 group-hover:opacity-100 text-slate-500 hover:text-red-400 transition-opacity"
                        >
                            <Trash2 size={16} />
                        </button>
                    </div>
                );
            }}
        />
    )}
    
    {/* 空状态 */}
    {filteredSettings.length === 0 && (
        <div className="text-center text-slate-500 text-sm mt-8 italic">
            {searchQuery ? "未找到匹配条目" : "暂无条目"}
        </div>
    )}
</div>
```

#### Step 3: 测试验证（30分钟）

**测试清单**:
- [ ] 添加100+测试设定条目
- [ ] 验证分类切换性能
- [ ] 验证搜索过滤性能
- [ ] 验证滚动流畅度
- [ ] 验证Echo状态指示器
- [ ] 验证删除功能
- [ ] 验证选择功能

**性能指标**:
- 滚动帧率: > 50fps (之前: < 15fps)
- 搜索响应: < 150ms (之前: 500-1000ms)

---

## Day 5-6: PlotWeaver 优化

### 执行步骤

#### Step 1: 定位渲染代码（20分钟）

```bash
# 搜索plotNodes.map的位置
grep -n "plotNodes.*\.map" components/PlotWeaver.tsx
```

预期位置：节点列表侧边栏或主画布

#### Step 2: 分析渲染模式（20分钟）

查找：
- 节点列表渲染
- 时间线渲染
- 是否有拖拽排序

#### Step 3: 重构为虚拟滚动（60分钟）

**预计修改**：
- 节点列表（侧边栏）：应用VirtualList
- 时间线（底部）：如节点过多，考虑虚拟化
- 保持拖拽功能兼容

**示例代码**:

```typescript
// 节点列表优化
<VirtualList
    items={sortedPlotNodes}
    itemHeight={120}
    height={600}
    renderItem={(node, idx) => (
        <PlotNodeCard 
            node={node} 
            draggable={true}
            onDragStart={handleDragStart}
        />
    )}
/>
```

#### Step 4: 测试验证（40分钟）

**测试清单**:
- [ ] 添加50+剧情节点
- [ ] 验证拖拽排序性能
- [ ] 验证滚动流畅度
- [ ] 验证节点连接性能
- [ ] 验证时间线渲染

---

## Day 7: 性能调优与总结

### 任务清单

#### 1. 安装react-window（可选）（30分钟）

```bash
npm install react-window @types/react-window
```

**收益**：
- 真正的虚拟滚动实现
- 更优的性能表现
- 更小的bundle size

#### 2. 性能基准测试（60分钟）

**测试场景**:
1. **CharacterCreator**: 50角色 + 搜索
2. **WorldBuilder**: 100设定 + 分类切换
3. **PlotWeaver**: 50节点 + 拖拽

**测试指标**:
- FPS（使用Chrome DevTools FPS meter）
- 搜索响应时间（console.time）
- 内存占用（Chrome DevTools Memory）
- React重渲染次数（React DevTools Profiler）

**基准对比表**:

| 组件 | 场景 | 优化前FPS | 优化后FPS | 提升 |
|------|------|----------|----------|------|
| CharacterCreator | 50角色滚动 | <20 | >50 | 2.5x+ |
| CharacterCreator | 搜索过滤 | 300ms | <100ms | 3x+ |
| WorldBuilder | 100设定滚动 | <15 | >50 | 3x+ |
| WorldBuilder | 分类切换 | 500ms | <150ms | 3x+ |
| PlotWeaver | 50节点拖拽 | <20 | >45 | 2x+ |

#### 3. 文档整理（30分钟）

- [ ] 更新组件文档，添加虚拟滚动说明
- [ ] 记录性能优化数据
- [ ] 创建最佳实践指南（何时使用VirtualList）

---

## 风险与应对

### 风险1: VirtualList 与现有交互冲突

**风险描述**：
- 拖拽排序可能失效
- Echo状态动画可能异常

**应对措施**：
- 保持itemHeight固定
- 使用renderItem的key属性
- 测试所有交互功能

### 风险2: 窗口大小变化导致布局问题

**风险描述**：
- 高度计算不准确
- 滚动条异常

**应对措施**：
- 使用window.innerHeight动态计算
- 监听resize事件重新渲染
- 设置min-height和max-height

### 风险3: react-window 未安装的降级问题

**风险描述**：
- 当前VirtualList已支持降级
- 但性能不如原生虚拟滚动

**应对措施**：
- 本周内安装react-window
- 测试降级模式与正常模式的差异

---

## 验收标准

### 功能验收
- [ ] 所有列表渲染正常，无空白/闪烁
- [ ] 搜索过滤功能正常
- [ ] Echo状态指示器正常显示
- [ ] 点击选择功能正常
- [ ] 拖拽功能正常（PlotWeaver）

### 性能验收
- [ ] 滚动帧率 > 45fps
- [ ] 搜索响应 < 200ms
- [ ] 无卡顿或延迟感
- [ ] 内存占用稳定，无泄漏

### 代码验收
- [ ] 代码整洁，注释清晰
- [ ] 无console.error/warn
- [ ] Lint检查通过
- [ ] TypeScript编译通过

---

## 后续建议

### 第2周预告：章节平衡算法

**前置准备**：
- 熟悉章节数据结构
- 准备测试用例（不同长度、类型、角色的章节）

**技术栈**：
- 纯前端算法（无需新增依赖）
- 可选：Chart.js/D3.js 用于可视化

---

## 执行确认

本周任务总时长：8-10小时

是否开始执行Day 1-2（CharacterCreator优化）？

确认后我将立即开始重构！
