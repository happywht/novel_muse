# 知识图谱深度可执行落地路线图 (Graph Execution Roadmap)

融合了先前的反哺分析与外部（系统级评估报告）的最佳实践，我们将大而全的方向收敛为**“可直接拆解为代码 Ticket”** 的深度落地路线图。

本方案共分为四个阶段（Phase），按照**依赖关系先后**与**ROI（投资回报率）高低**进行排序。这不仅是一份架构蓝图，更是一份可以直接导入 Jira/Github Issues 的行动清单。

---

## Phase 1: 粒度精炼与逻辑收口 (Foundation & Integrity)
**目标**：将粗糙的图谱能力提升至工业级精度，确保底层数据与设定的绝对一致，解决“干瘪的文本”问题。

### 🎫 Task 1.1: 情感强度与状态量化体系 (P0)
**现状**：目前图谱的边（如 `LOVES`, `ENEMY_OF`）是单薄的二元关系，大模型无法判断“微有好感”和“生死相许”的区别。
**执行步骤**：
1. **Schema 变更**：修改 [neo4jService.ts](file:///d:/%E5%AE%B6%E5%BA%AD/%E5%89%AF%E4%B8%9A%E6%8E%A2%E7%B4%A2/%E5%B0%8F%E8%AF%B4%E9%A1%B9%E7%9B%AE/%E5%B0%8F%E8%AF%B4%E5%BC%80%E9%A2%98/remix_-muse_-%E5%B0%8F%E8%AF%B4%E6%9E%B6%E6%9E%84%E5%B8%88_022302/server/src/services/neo4jService.ts) 中的存图逻辑，为关系边注入 `weight` (0~100) 和 `trajectory` (rising, falling, stable) 属性。
2. **API 改造**：在 [extractEchoesFromText](file:///d:/%E5%AE%B6%E5%BA%AD/%E5%89%AF%E4%B8%9A%E6%8E%A2%E7%B4%A2/%E5%B0%8F%E8%AF%B4%E9%A1%B9%E7%9B%AE/%E5%B0%8F%E8%AF%B4%E5%BC%80%E9%A2%98/remix_-muse_-%E5%B0%8F%E8%AF%B4%E6%9E%B6%E6%9E%84%E5%B8%88_022302/services/geminiService.ts#1109-1218) 时，提示词需强制赋予关系一个推测权重。
3. **Cypher 演进**：查询时过滤阈值（例如：仅将 weight > 60 的仇恨关系视为致命冲突）。
**预期结果**：建立具有厚度的人际关系网络，为后续的情节张力提供具体数值支撑。

### 🎫 Task 1.2: 强空间逻辑拦截器 (P1)
**现状**：AI 在长篇幅中依然会犯“瞬移”和“死人复活”的物理法则错误。
**执行步骤**：
1. **提取端拦截**：在生成新的一章之前，提取本章提及的实体与地点。
2. **校验端 [verifyLogic](file:///d:/%E5%AE%B6%E5%BA%AD/%E5%89%AF%E4%B8%9A%E6%8E%A2%E7%B4%A2/%E5%B0%8F%E8%AF%B4%E9%A1%B9%E7%9B%AE/%E5%B0%8F%E8%AF%B4%E5%BC%80%E9%A2%98/remix_-muse_-%E5%B0%8F%E8%AF%B4%E6%9E%B6%E6%9E%84%E5%B8%88_022302/services/geminiService.ts#1784-1808)**：写一段 Cypher，查询这些实体当前的 `[:LOCATED_IN]` 或 `[:ALIVE]` 状态。
3. **UI 反馈**：若图谱返回“该角色当前位于深渊，不可能出现在酒馆”，系统在 Draft 编辑器中标红提示，阻止脏数据被 `ACCEPT` 进入图谱。

---

## Phase 2: 沙盘推演与伏笔闭环 (Sandbox & Foreshadowing)
**目标**：解决“流水账”与“挖坑不填”，让 AI 具备小说家的“草蛇灰线”能力。

### 🎫 Task 2.1: 悬置伏笔追踪与强制回收系统 (P0)
**核心思想**：伏笔是最高级别的“待执行约束”。
**执行步骤**：
1. **UI 入口**：在 Drafting Room 的右侧/或选取文本时，增加“标记为伏笔”按钮。
2. **图谱建模**：创建一个特殊的半连接节点 `(Event)-[:FORESHADOWS]->(Unknown/Future)` 或挂载 `isUnresolvedThread = true` 标签。
3. **上下文挟持**：在后续章节生成的上下文中，定期由图引擎查询 `MATCH (n) WHERE n.isUnresolvedThread = true RETURN n`，如果主角进入了伏笔相关的地点/人物圈，以最强烈的 System Prompt 指令要求 LLM **[必须在本章填坑：收回线索 X]**。
4. **闭环核账**：由阶段一的逻辑校验器判断这章是否真的回收了，若回收，消除该图谱节点的 `isUnresolvedThread`。

### 🎫 Task 2.2: What-If 动态分支沙盘 (P1)
**核心思想**：“如果主角不这么做，会怎样？”
**执行步骤**：
1. **前端重构**：升级“灵感跳跃(Twists)”功能。允许用户输入一个假设动作，例如“杀掉公爵”。
2. **图算法引擎**：后台执行 Shortest Path 图分析，计算“公爵”节点的所有的直系亲属、政治盟友及欠下的人情。
3. **LLM 解释器**：将这条波及的“影响链”发给大模型翻译：“由于您杀了公爵，他的后台老国王会在 3 章后发难，而他的私生子将立刻与您的敌对家族结盟。”——为作者提供数据级别的剧情排雷与分支建议。

---

## Phase 3: 子图注入极限压榨 (Infinite Context Injection)
**目标**：低成本、无幻觉的 100+ 章连载续航。

### 🎫 Task 3.1: 智能高维 RAG 子图抽取器 (P0)
**现状**：目前可能查询范围过泛，或者带入了无关节点。
**执行步骤**：
1. **多中心锚点算法**：目前的章节不仅仅围绕单个主角，可能围绕 `[主角, 配角A, 遗迹B]` 三个锚点。
2. **交集与扩散优化**：在 Neo4j 中获取这三个锚点的 1-hop 与核心 2-hop，但**过滤低频长尾节点**（权重法）。
3. **Prompt 封装**：将提取到的 [(A)-[恨]->(B)](file:///d:/%E5%AE%B6%E5%BA%AD/%E5%89%AF%E4%B8%9A%E6%8E%A2%E7%B4%A2/%E5%B0%8F%E8%AF%B4%E9%A1%B9%E7%9B%AE/%E5%B0%8F%E8%AF%B4%E5%BC%80%E9%A2%98/remix_-muse_-%E5%B0%8F%E8%AF%B4%E6%9E%B6%E6%9E%84%E5%B8%88_022302/App.tsx#23-265) 纯关系骨架组装在 Prompt L3 层顶部，实现“极简而核心的世界观即时注入”。

### 🎫 Task 3.2: 章节级元图谱 (Chapter Meta-Graph) (P2)
**核心思想**：大纲可视化的终极形态。将每一章本身也作为一个大节点存入图谱。
**执行步骤**：
1. 建立 `(:Chapter {id: 1})` 节点。
2. 连接关系：`(:Chapter 1)-[:INTRODUCES]->(:Character A)`, `(:Chapter 5)-[:RESOLVES_CONFLICT]->(:Event B)`。
3. 效果：作者可以在专门的视图中，俯瞰自己的大纲结构，看到每一章的情绪起伏、出场密度和因果关系链。

---

## 🚀 建议第一步 Action：

如果立刻动工，我强烈推荐我们从 **Phase 2 的 `Task 2.1: 悬置伏笔追踪与强制回收系统`** 或者是 **Phase 1 的 `Task 1.1: 情感强度量化`** 开始着手。

- **做伏笔系统**：能立刻感受到小说具备了跨章节的记忆勾稽，效果拔群。
- **做情感量化**：是对底层 Schema 最小但最硬核的改造，它是做好复杂洞察（修罗场等）的数据地基。

你希望我们从哪一个 Task 切入（撕开口子）？
